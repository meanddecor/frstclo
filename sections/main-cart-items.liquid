{% comment %}
✅ GLOBAL SETTINGS APPLIED - INDIVIDUAL TEXT ELEMENT CONTROLS:
- Product Title: title_font, title_font_size, title_font_weight, title_text_transform, title_italic, title_underline, title_color, title_letter_spacing
- Price: price_font, price_font_size, price_font_weight, price_italic, price_color, price_letter_spacing
- Quantity: quantity_font, quantity_font_size, quantity_font_weight, quantity_italic, quantity_color, quantity_letter_spacing, quantity_button_color, quantity_button_border_color
- Remove Link: remove_link_font, remove_link_font_size, remove_link_font_weight, remove_link_italic, remove_link_color, remove_link_hover_color, remove_link_underline, remove_link_letter_spacing
- Cart Total: total_font, total_font_size, total_font_weight, total_italic, total_color, total_letter_spacing
- Checkout Button: button_font, button_font_size, button_font_weight, button_text_transform, button_italic, button_text_color, button_bg_color, button_letter_spacing
- Spacing: item_spacing, padding_top, padding_bottom
- Every text element has independent font picker, size, weight, color, italic, and spacing controls
- Quantity +/- button icons and borders are fully customizable
{% endcomment %}

<style>
  {{ section.settings.title_font | font_face: font_display: 'swap' }}
  {{ section.settings.price_font | font_face: font_display: 'swap' }}
  {{ section.settings.quantity_font | font_face: font_display: 'swap' }}
  {{ section.settings.remove_link_font | font_face: font_display: 'swap' }}
  {{ section.settings.total_font | font_face: font_display: 'swap' }}
  {{ section.settings.button_font | font_face: font_display: 'swap' }}
</style>

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .cart {
    flex: 1;
    padding: 0;
  }

  .cart__form {
    margin-bottom: 5rem;
    display: flex;
    flex-direction: column;
  }

  .cart__items {
    max-width: 1200px;
    margin: 0 auto;
    width: 100%;
    padding: 0 16px;
  }

  @media screen and (min-width: 750px) {
    .cart__items {
      padding: 0 20px;
    }
  }

  .cart__item {
    width: 100%;
    display: flex;
    align-items: center;
    gap: 20px;
    padding: 20px 0;
    border-bottom: 1px solid #e5e5e5;
    margin-bottom: {{ section.settings.item_spacing }}px;
  }

  @media screen and (max-width: 748px) {
    .cart__item {
      flex-wrap: wrap;
      gap: 12px;
      padding: 16px 0;
    }

    .cart__item-img {
      width: 80px;
      height: 80px;
    }

    .cart__item-heading {
      flex: 1;
      min-width: 0;
    }

    .cart__item-title {
      font-size: 14px !important;
      line-height: 1.3 !important;
    }

    .cart__item-final-price {
      width: 100%;
      margin: 8px 0 0 0;
      text-align: left;
      font-size: 16px !important;
    }

    .cart__item-quantity {
      width: 100%;
      justify-content: flex-start;
    }

    .checkout-elements {
      align-items: center;
      padding: 20px 16px;
      max-width: 100%;
      margin: 40px auto 20px;
      gap: 20px;
    }

    .cart__submit-btn {
      width: 100%;
      padding: 16px 30px !important;
      font-size: 14px !important;
    }

    .cart__total {
      font-size: 18px !important;
    }
  }

  @media screen and (min-width: 749px) {
    .cart__item {
      width: 100%;
      gap: 30px;
    }
  }

  .cart__item-img {
    width: 120px;
    height: 120px;
    object-fit: cover;
    flex-shrink: 0;
  }

  .cart__item-heading {
    display: flex;
    flex-direction: column;
    gap: 8px;
    flex: 1;
  }

  .cart__item-title {
    margin: 0;
    font-family: {{ section.settings.title_font.family }}, {{ section.settings.title_font.fallback_families }};
    font-weight: {{ section.settings.title_font.weight }};
    font-style: {{ section.settings.title_font.style }};
    font-size: {{ section.settings.title_font_size }}px;
    line-height: 1.4;
    color: {{ section.settings.title_color }} !important;
    letter-spacing: {{ section.settings.title_letter_spacing }}px;
    text-transform: {{ section.settings.title_text_transform }};
    {% if section.settings.title_italic %}font-style: italic;{% endif %}
    {% if section.settings.title_underline %}text-decoration: underline;{% endif %}
  }

  .cart__item-remove-link {
    font-family: {{ section.settings.remove_link_font.family }}, {{ section.settings.remove_link_font.fallback_families }};
    font-weight: {{ section.settings.remove_link_font_weight }};
    font-style: {{ section.settings.remove_link_font.style }};
    font-size: {{ section.settings.remove_link_font_size }}px;
    color: {{ section.settings.remove_link_color }} !important;
    {% if section.settings.remove_link_underline %}text-decoration: underline !important;{% else %}text-decoration: none !important;{% endif %}
    text-transform: uppercase;
    letter-spacing: {{ section.settings.remove_link_letter_spacing }}px;
    {% if section.settings.remove_link_italic %}font-style: italic;{% endif %}
    cursor: pointer;
  }

  .cart__item-remove-link:hover {
    color: {{ section.settings.remove_link_hover_color }} !important;
  }

  .cart__item-final-price {
    font-family: {{ section.settings.price_font.family }}, {{ section.settings.price_font.fallback_families }};
    font-weight: {{ section.settings.price_font_weight }};
    font-style: {{ section.settings.price_font.style }};
    font-size: {{ section.settings.price_font_size }}px;
    line-height: 1.2;
    color: {{ section.settings.price_color }} !important;
    letter-spacing: {{ section.settings.price_letter_spacing }}px;
    {% if section.settings.price_italic %}font-style: italic;{% endif %}
    white-space: nowrap;
    margin: 0 20px 0 auto;
  }

  .cart__item-quantity {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .cart__item-title-span {
    display: none;
  }

  .cart__item-main-span {
    display: inline;
    color: {{ section.settings.price_color }} !important;
  }
  
  .cart__item-final-price .cart__item-main-span {
    color: inherit !important;
  }

  .quantity-wrapper {
    display: flex;
    align-items: center;
    gap: 12px;
    border: 1px solid {{ section.settings.quantity_button_border_color }};
    background-color: {{ section.settings.quantity_wrapper_bg_color }};
    padding: 8px 12px;
    border-radius: 4px;
  }

  @media screen and (max-width: 748px) {
    .quantity-wrapper {
      padding: 10px 14px;
      gap: 14px;
    }
  }

  .cart__quantity-btn {
    padding: 4px;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    background: none;
    transition: opacity 0.2s;
  }

  .cart__quantity-btn:hover:not(:disabled) {
    opacity: 0.7;
  }

  .cart__quantity-btn-minus.disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }

  button.cart__quantity-btn svg {
    width: 16px;
    height: 16px;
  }

  @media screen and (max-width: 748px) {
    button.cart__quantity-btn svg {
      width: 18px;
      height: 18px;
    }
  }

  button.cart__quantity-btn svg path {
    fill: {{ section.settings.quantity_button_color }};
  }

  .cart__quantity-value {
    min-width: 20px;
    text-align: center;
    font-family: {{ section.settings.quantity_font.family }}, {{ section.settings.quantity_font.fallback_families }};
    font-weight: {{ section.settings.quantity_font_weight }};
    font-style: {{ section.settings.quantity_font.style }};
    font-size: {{ section.settings.quantity_font_size }}px;
    color: {{ section.settings.quantity_color }} !important;
    letter-spacing: {{ section.settings.quantity_letter_spacing }}px;
    {% if section.settings.quantity_italic %}font-style: italic;{% endif %}
  }

  .checkout-elements {
    max-width: 400px;
    margin: 60px auto 40px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 30px;
    padding: 0;
  }

  .cart__total {
    font-family: {{ section.settings.total_font.family }}, {{ section.settings.total_font.fallback_families }} !important;
    font-weight: {{ section.settings.total_font_weight }} !important;
    font-style: {{ section.settings.total_font.style }} !important;
    font-size: {{ section.settings.total_font_size }}px !important;
    color: {{ section.settings.total_color }} !important;
    margin: 0;
    letter-spacing: {{ section.settings.total_letter_spacing }}px !important;
    text-align: center;
    {% if section.settings.total_italic %}font-style: italic;{% endif %}
  }

  .cart__submit-btn {
    padding: 18px 60px !important;
    font-family: {{ section.settings.button_font.family }}, {{ section.settings.button_font.fallback_families }};
    font-weight: {{ section.settings.button_font_weight }};
    font-style: {{ section.settings.button_font.style }};
    font-size: {{ section.settings.button_font_size }}px;
    color: {{ section.settings.button_text_color }};
    background: {{ section.settings.button_bg_color }};
    text-transform: {{ section.settings.button_text_transform }};
    {% if section.settings.button_italic %}font-style: italic;{% endif %}
    border: none;
    cursor: pointer;
    letter-spacing: {{ section.settings.button_letter_spacing }}px;
    transition: background 0.3s;
  }

  .cart__submit-btn:hover {
    background: #333;
  }

  .title-wrapper-with-link {
    max-width: 1200px;
    margin: 0 auto 30px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 16px;
    border-bottom: 1px solid #e5e5e5;
    gap: 12px;
  }

  @media screen and (min-width: 750px) {
    .title-wrapper-with-link {
      padding: 20px 20px;
    }
  }

  @media screen and (max-width: 748px) {
    .title-wrapper-with-link {
      flex-direction: column;
      align-items: flex-start;
      gap: 12px;
      padding: 16px;
    }
  }

  .title--primary {
    font-size: 28px;
    font-weight: 600;
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  @media screen and (max-width: 748px) {
    .title--primary {
      font-size: 20px;
      letter-spacing: 0.5px;
    }
  }

  .underlined-link {
    font-size: 14px;
    text-decoration: underline;
    color: #000;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    white-space: nowrap;
  }

  @media screen and (max-width: 748px) {
    .underlined-link {
      font-size: 12px;
    }
  }

  .underlined-link:hover {
    opacity: 0.7;
  }

  .cart__warnings {
    max-width: 1200px;
    margin: 40px auto;
    padding: 0 16px;
    text-align: center;
  }

  @media screen and (min-width: 750px) {
    .cart__warnings {
      padding: 0 20px;
    }
  }

  .cart__empty-text {
    font-size: 24px;
    margin-bottom: 30px;
  }

  @media screen and (max-width: 748px) {
    .cart__empty-text {
      font-size: 18px;
      margin-bottom: 20px;
    }
  }
{%- endstyle -%}

{%- unless settings.cart_type == 'drawer' -%}
  <script src="{{ 'cart.js' | asset_url }}" defer="defer"></script>
{%- endunless -%}

<script>
  if (typeof window !== 'undefined') {
    document.addEventListener('DOMContentLoaded', function() {
      const quantityWrappers = document.querySelectorAll('.quantity-wrapper');
      
      if (!quantityWrappers || quantityWrappers.length === 0) return;
      
      quantityWrappers.forEach(wrapper => {
        if (!wrapper) return;
        
        const minusBtn = wrapper.querySelector('.cart__quantity-btn-minus');
        const plusBtn = wrapper.querySelector('.cart__quantity-btn-plus');
        const quantityValue = wrapper.querySelector('.cart__quantity-value');
        const quantityInput = wrapper.querySelector('input[data-quantity-input]');
        
        if (!minusBtn || !plusBtn || !quantityValue || !quantityInput) return;
        
        minusBtn.addEventListener('click', function(e) {
          e.preventDefault();
          let currentQty = parseInt(quantityValue.textContent);
          if (currentQty > 1) {
            currentQty--;
            quantityValue.textContent = currentQty;
            quantityInput.value = currentQty;
            
            if (currentQty === 1) {
              minusBtn.classList.add('disabled');
              minusBtn.disabled = true;
            }
            
            updateLinePrice(wrapper, currentQty);
            updateCart();
          }
        });
        
        plusBtn.addEventListener('click', function(e) {
          e.preventDefault();
          let currentQty = parseInt(quantityValue.textContent);
          currentQty++;
          quantityValue.textContent = currentQty;
          quantityInput.value = currentQty;
          
          if (currentQty > 1) {
            minusBtn.classList.remove('disabled');
            minusBtn.disabled = false;
          }
          
          updateLinePrice(wrapper, currentQty);
          updateCart();
        });
      });
      
      function updateLinePrice(wrapper, quantity) {
        const unitPrice = parseFloat(wrapper.dataset.unitPrice);
        const linePrice = quantity * unitPrice;
        const linePriceElement = wrapper.closest('.cart__item').querySelector('.cart__item-main-span');
        
        // Format as currency
        const formattedPrice = new Intl.NumberFormat('en-US', {
          style: 'currency',
          currency: 'USD'
        }).format(linePrice / 100);
        
        linePriceElement.textContent = formattedPrice;
        
        // Update cart total
        updateCartTotal();
      }
      
      function updateCartTotal() {
        const form = document.querySelector('.cart__form');
        if (!form) return;
        
        let total = 0;
        const wrappers = form.querySelectorAll('.quantity-wrapper');
        wrappers.forEach(wrapper => {
          const unitPrice = parseFloat(wrapper.dataset.unitPrice);
          const quantity = parseInt(wrapper.querySelector('.cart__quantity-value').textContent);
          total += unitPrice * quantity;
        });
        
        const totalElement = document.querySelector('.cart__total');
        if (totalElement) {
          const formattedTotal = new Intl.NumberFormat('en-US', {
            style: 'currency',
            currency: 'USD'
          }).format(total / 100);
          totalElement.textContent = 'TOTAL: ' + formattedTotal + ' USD';
        }
      }
      
      function updateCart() {
        const form = document.querySelector('.cart__form');
        if (!form) return;
        
        const inputs = form.querySelectorAll('input[data-quantity-input]');
        if (!inputs || inputs.length === 0) return;
        
        const updates = {};
        inputs.forEach(input => {
          const wrapper = input.closest('.quantity-wrapper');
          if (wrapper && wrapper.dataset.key) {
            updates[wrapper.dataset.key] = parseInt(input.value) || 0;
          }
        });
        
        fetch('/cart/update', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({ updates: updates })
        })
        .then(response => {
          if (!response.ok) throw new Error('Network response was not ok');
          return response.json();
        })
        .then(data => {
          location.reload();
        })
        .catch(error => {
          console.error('Cart update error:', error);
        });
      }
    });
  }
</script>

<cart-items class="page-width{% if cart == empty %} is-empty{% else %} section-{{ section.id }}-padding{% endif %}">
  <div class="title-wrapper-with-link">
    <h1 class="title title--primary">{{ 'sections.cart.title' | t }}</h1>
    <a href="{{ routes.all_products_collection_url }}" class="underlined-link">{{ 'general.continue_shopping' | t }}</a>
  </div>

  <div class="cart__warnings">
    <h1 class="cart__empty-text">{{ 'sections.cart.empty' | t }}</h1>
    <a href="{{ routes.all_products_collection_url }}" class="button">
      {{ 'general.continue_shopping' | t }}
    </a>

    {%- if shop.customer_accounts_enabled and customer == null -%}
      <h2 class="cart__login-title">{{ 'sections.cart.login.title' | t }}</h2>
      <p class="cart__login-paragraph">
        {{ 'sections.cart.login.paragraph_html' | t: link: routes.account_login_url }}
      </p>
    {%- endif -%}
  </div>

  <form action="{{ routes.cart_url }}" class="cart__form" method="post" id="cart">
    <div class="cart__items" id="main-cart-items" data-id="{{ section.id }}">
      <div class="js-contents">
        {%- if cart != empty -%}
          {%- for item in cart.items -%}
            <div class="cart__item" id="CartItem-{{ item.index | plus: 1 }}">
              {% if item.image %}
                <img
                  class="cart__item-img"
                  src="{{ item.image | image_url: width: 300 }}"
                  alt="{{ item.image.alt | escape }}"
                  loading="lazy"
                >
              {% endif %}

              <div class="cart__item-heading">
                <h2 class="cart__item-title">{{ item.product.title | escape }}</h2>
                <a class="cart__item-remove-link" href="{{ item.url_to_remove }}">Remove</a>
              </div>

              <p class="cart__item-final-price" data-unit-price="{{ item.final_price }}">
                <span class="cart__item-title-span">Price</span>
                <span class="cart__item-main-span" data-line-price="{{ item.key }}">{{ item.final_line_price | money }}</span>
              </p>

              <div class="cart__item-quantity">
                <span class="cart__item-title-span">Quantity</span>
                <div class="quantity-wrapper" 
                     data-key="{{ item.key }}" 
                     data-quantity="{{ item.quantity }}"
                     data-unit-price="{{ item.final_price }}"
                     data-inventory="0"
                     data-track-quantity="false"
                     data-allow-oversell="deny">
                  <button class="cart__quantity-btn cart__quantity-btn-minus{% if item.quantity <= 1 %} disabled{% endif %}" 
                          type="button"
                          {% if item.quantity <= 1 %}disabled{% endif %}>
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none">
                      <path d="M21 12C21 12.1989 20.921 12.3897 20.7803 12.5303C20.6397 12.671 20.4489 12.75 20.25 12.75H3.75C3.55109 12.75 3.36032 12.671 3.21967 12.5303C3.07902 12.3897 3 12.1989 3 12C3 11.8011 3.07902 11.6103 3.21967 11.4697C3.36032 11.329 3.55109 11.25 3.75 11.25H20.25C20.4489 11.25 20.6397 11.329 20.7803 11.4697C20.921 11.6103 21 11.8011 21 12Z"></path>
                    </svg>
                  </button>
                  <input type="hidden" name="updates[]" value="{{ item.quantity }}" data-quantity-input>
                  <span class="cart__quantity-value" id="quantity-{{ item.key }}">{{ item.quantity }}</span>
                  <button class="cart__quantity-btn cart__quantity-btn-plus" type="button">
                    <svg xmlns="http://www.w3.org/2000/svg" width="25" height="25" viewBox="0 0 25 25" fill="none">
                      <path d="M21.4355 12.6982C21.4355 12.8972 21.3565 13.0879 21.2159 13.2286C21.0752 13.3692 20.8845 13.4482 20.6855 13.4482H13.1855V20.9482C13.1855 21.1472 13.1065 21.3379 12.9659 21.4786C12.8252 21.6192 12.6345 21.6982 12.4355 21.6982C12.2366 21.6982 12.0459 21.6192 11.9052 21.4786C11.7646 21.3379 11.6855 21.1472 11.6855 20.9482V13.4482H4.18555C3.98663 13.4482 3.79587 13.3692 3.65522 13.2286C3.51456 13.0879 3.43555 12.8972 3.43555 12.6982C3.43555 12.4993 3.51456 12.3086 3.65522 12.1679C3.79587 12.0273 3.98663 11.9482 4.18555 11.9482H11.6855V4.44824C11.6855 4.24933 11.7646 4.05856 11.9052 3.91791C12.0459 3.77726 12.2366 3.69824 12.4355 3.69824C12.6345 3.69824 12.8252 3.77726 12.9659 3.91791C13.1065 4.05856 13.1855 4.24933 13.1855 4.44824V11.9482H20.6855C20.8845 11.9482 21.0752 12.0273 21.2159 12.1679C21.3565 12.3086 21.4355 12.4993 21.4355 12.6982Z"></path>
                    </svg>
                  </button>
                </div>
              </div>
            </div>
          {%- endfor -%}
        {%- endif -%}
      </div>
    </div>

    <div class="checkout-elements">
      <p class="cart__total">TOTAL: {{ cart.total_price | money }} USD</p>
      <button type="submit" name="checkout" class="cart__submit-btn button">
        CHECKOUT
      </button>
    </div>

    <p class="visually-hidden" id="cart-live-region-text" aria-live="polite" role="status"></p>
    <p class="visually-hidden" id="shopping-cart-line-item-status" aria-live="polite" aria-hidden="true" role="status">
      {{ 'accessibility.loading' | t }}
    </p>
  </form>
</cart-items>

{% schema %}
{
  "name": "t:sections.main-cart-items.name",
  "settings": [
    {
      "type": "header",
      "content": "Product Title Typography"
    },
    {
      "type": "font_picker",
      "id": "title_font",
      "label": "Title Font",
      "default": "assistant_n4"
    },
    {
      "type": "range",
      "id": "title_font_size",
      "min": 12,
      "max": 32,
      "step": 1,
      "unit": "px",
      "label": "Title Font Size",
      "default": 16
    },
    {
      "type": "select",
      "id": "title_font_weight",
      "label": "Title Font Weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Normal" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi Bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "500"
    },
    {
      "type": "select",
      "id": "title_text_transform",
      "label": "Title Text Transform",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "uppercase", "label": "Uppercase" },
        { "value": "lowercase", "label": "Lowercase" },
        { "value": "capitalize", "label": "Capitalize" }
      ],
      "default": "uppercase"
    },
    {
      "type": "checkbox",
      "id": "title_italic",
      "label": "Title Italic",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "title_underline",
      "label": "Title Underline",
      "default": false
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "title_letter_spacing",
      "min": -2,
      "max": 5,
      "step": 0.1,
      "unit": "px",
      "label": "Title Letter Spacing",
      "default": 0.5
    },
    {
      "type": "header",
      "content": "Price Typography"
    },
    {
      "type": "font_picker",
      "id": "price_font",
      "label": "Price Font",
      "default": "assistant_n4"
    },
    {
      "type": "range",
      "id": "price_font_size",
      "min": 10,
      "max": 32,
      "step": 1,
      "unit": "px",
      "label": "Price Font Size",
      "default": 14
    },
    {
      "type": "select",
      "id": "price_font_weight",
      "label": "Price Font Weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Normal" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi Bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "400"
    },
    {
      "type": "checkbox",
      "id": "price_italic",
      "label": "Price Italic",
      "default": false
    },
    {
      "type": "color",
      "id": "price_color",
      "label": "Price Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "price_letter_spacing",
      "min": -2,
      "max": 5,
      "step": 0.1,
      "unit": "px",
      "label": "Price Letter Spacing",
      "default": 0
    },
    {
      "type": "header",
      "content": "Quantity Typography"
    },
    {
      "type": "font_picker",
      "id": "quantity_font",
      "label": "Quantity Font",
      "default": "assistant_n4"
    },
    {
      "type": "range",
      "id": "quantity_font_size",
      "min": 10,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Quantity Font Size",
      "default": 14
    },
    {
      "type": "select",
      "id": "quantity_font_weight",
      "label": "Quantity Font Weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Normal" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi Bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "500"
    },
    {
      "type": "checkbox",
      "id": "quantity_italic",
      "label": "Quantity Italic",
      "default": false
    },
    {
      "type": "color",
      "id": "quantity_color",
      "label": "Quantity Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "quantity_letter_spacing",
      "min": -2,
      "max": 5,
      "step": 0.1,
      "unit": "px",
      "label": "Quantity Letter Spacing",
      "default": 0
    },
    {
      "type": "color",
      "id": "quantity_button_color",
      "label": "Quantity Button Icon Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "quantity_button_border_color",
      "label": "Quantity Button Border Color",
      "default": "#e5e5e5"
    },
    {
      "type": "color",
      "id": "quantity_wrapper_bg_color",
      "label": "Quantity Wrapper Background Color",
      "default": "#ffffff"
    },
    {
      "type": "header",
      "content": "Remove Link Typography"
    },
    {
      "type": "font_picker",
      "id": "remove_link_font",
      "label": "Remove Link Font",
      "default": "assistant_n4"
    },
    {
      "type": "range",
      "id": "remove_link_font_size",
      "min": 10,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Remove Link Font Size",
      "default": 12
    },
    {
      "type": "select",
      "id": "remove_link_font_weight",
      "label": "Remove Link Font Weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Normal" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi Bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "400"
    },
    {
      "type": "checkbox",
      "id": "remove_link_italic",
      "label": "Remove Link Italic",
      "default": false
    },
    {
      "type": "color",
      "id": "remove_link_color",
      "label": "Remove Link Color",
      "default": "#0066cc"
    },
    {
      "type": "color",
      "id": "remove_link_hover_color",
      "label": "Remove Link Hover Color",
      "default": "#0052a3"
    },
    {
      "type": "checkbox",
      "id": "remove_link_underline",
      "label": "Remove Link Underline",
      "default": true
    },
    {
      "type": "range",
      "id": "remove_link_letter_spacing",
      "min": -2,
      "max": 5,
      "step": 0.1,
      "unit": "px",
      "label": "Remove Link Letter Spacing",
      "default": 0.5
    },
    {
      "type": "header",
      "content": "Cart Total Typography"
    },
    {
      "type": "font_picker",
      "id": "total_font",
      "label": "Total Font",
      "default": "assistant_n4"
    },
    {
      "type": "range",
      "id": "total_font_size",
      "min": 14,
      "max": 36,
      "step": 1,
      "unit": "px",
      "label": "Total Font Size",
      "default": 20
    },
    {
      "type": "select",
      "id": "total_font_weight",
      "label": "Total Font Weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Normal" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi Bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "600"
    },
    {
      "type": "checkbox",
      "id": "total_italic",
      "label": "Total Italic",
      "default": false
    },
    {
      "type": "color",
      "id": "total_color",
      "label": "Total Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "total_letter_spacing",
      "min": -2,
      "max": 5,
      "step": 0.1,
      "unit": "px",
      "label": "Total Letter Spacing",
      "default": 0.5
    },
    {
      "type": "header",
      "content": "Checkout Button Typography"
    },
    {
      "type": "font_picker",
      "id": "button_font",
      "label": "Button Font",
      "default": "assistant_n4"
    },
    {
      "type": "range",
      "id": "button_font_size",
      "min": 12,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Button Font Size",
      "default": 16
    },
    {
      "type": "select",
      "id": "button_font_weight",
      "label": "Button Font Weight",
      "options": [
        { "value": "300", "label": "Light" },
        { "value": "400", "label": "Normal" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi Bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "600"
    },
    {
      "type": "select",
      "id": "button_text_transform",
      "label": "Button Text Transform",
      "options": [
        { "value": "none", "label": "None" },
        { "value": "uppercase", "label": "Uppercase" },
        { "value": "lowercase", "label": "Lowercase" },
        { "value": "capitalize", "label": "Capitalize" }
      ],
      "default": "uppercase"
    },
    {
      "type": "checkbox",
      "id": "button_italic",
      "label": "Button Italic",
      "default": false
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button Text Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_bg_color",
      "label": "Button Background Color",
      "default": "#000000"
    },
    {
      "type": "range",
      "id": "button_letter_spacing",
      "min": -2,
      "max": 5,
      "step": 0.1,
      "unit": "px",
      "label": "Button Letter Spacing",
      "default": 1
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "item_spacing",
      "min": 10,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Space Between Items",
      "default": 10
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ]
}
{% endschema %}
